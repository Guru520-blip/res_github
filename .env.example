# Required
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Email finding — get free keys at hunter.io and apollo.io
HUNTER_API_KEY=
APOLLO_API_KEY=

# Email verification — get free key at abstractapi.com
ABSTRACT_API_KEY=

# Email sending (optional — for direct send feature)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=
FROM_NAME=

# Web discovery (optional — 100 free/month at serpapi.com)
SERP_API_KEY=

# Max emails per day (safety limit)
MAX_EMAILS_PER_DAY=15
